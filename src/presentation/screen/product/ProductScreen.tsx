import React from 'react';
import {View, Text} from 'react-native';

export const ProductScreen = () => {
  // Listado de Productos
  // Tocas en uno de ellos (id) => te lleva al productIdScreen. Podes Eliminar o Editar
  // Al Tocar eliminar (lo eliminas). Al tocar Editar te cambia el estado

  // Agregar nuevo te llava a la pantalla de NewProductScreen y ahi al producto que vas a crear le asignas una categoria ya existente

  return (
    <View>
      <Text>ProductScreen</Text>
    </View>
  );
};
